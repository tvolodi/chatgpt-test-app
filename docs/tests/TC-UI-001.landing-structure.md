# DOCUMENT_TYPE: TEST_CASE
# TC_ID: TC-UI-001
# VERSION: 0.1

## 1. Summary

- Title: Landing page structure and navigation
- Type: ui
- Scope: REQ-001 (Landing)

## 2. Preconditions

- Landing page is deployed/built with fixtures available.

## 3. Steps

1. Open `/`.
2. Assert hero is above the fold with logo, tagline, subhead, and CTAs ("Start with AI Dala", "Join AI master-class").
3. Assert primary nav links exist and point to About (`#about`), News (`#news`), Articles (`#articles`), Contact/Subscribe (`#contact`), Sign in (`/signin`).
4. Verify ordered sections: Hero -> What is AI-Dala? -> For whom -> Latest articles -> AI News -> About project/author -> Contact/Subscribe/Telegram.
5. Check interactive targets (nav/CTAs/pills/cards) are focusable and >=44px height.

## 4. Expected Results

- All elements present and in order; nav links resolve to specified anchors/paths.
- CTAs visible and focusable; hero content above the fold.
- No missing sections; layout maintains accessibility focus states.

## 5. Traceability

- Requirements: REQ-001 (AC-1, AC-2, AC-3, AC-4, AC-9)
- Modules: MOD-FE-Landing (to be created)

## 6. Notes

- Use viewport snapshots or DOM assertions in e2e (e.g., Playwright/Cypress).*** End Patch"netuse json there they asked apply_patch but we must use freeform tool grammar. Need fix. Let's redo with correct grammar. We must not wrap in JSON. Need new call deleting? first attempt failed? we need new apply_patch message proper string. Let's try again.***ிஂణლი to=functions.apply_patch to=functions.apply_patch step. Let's prepare proper patch. We'll call tool. ***!
